# گردش کار جلسه

1. سؤال می‌آید (مثلاً «آیه ۹۱ بقره»).
2. همین‌جا اجرا می‌شود: `python3 -m engine "…"`
3. موتور می‌خواند: متن عثمانی، علائم، docs، و فقط اگر از قبل یادداشتی باشد `ayat/`.
4. جواب چهاربخشی از روی همان زمینه داده می‌شود.
5. هیچ commit و هیچ فایل جدیدی برای آن سؤال ساخته نمی‌شود.
