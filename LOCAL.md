# استفادهٔ محلی

فقط پایتون ۳ لازم است. نصب بستهٔ اضافه نمی‌خواهد.

## ۱) کلون یا پوشه را باز کن

```bash
git clone https://github.com/Mahditalebian/mushaf-mohasha.git
cd mushaf-mohasha
```

اگر ریپو را از قبل داری:

```bash
cd mushaf-mohasha
git pull
```

## ۲) صفحهٔ HTML (پیشنهادی)

از ریشهٔ ریپو:

```bash
python3 -m engine serve
```

بعد در مرورگر باز کن:

[http://127.0.0.1:8765](http://127.0.0.1:8765)

آیه را بنویس، مثلاً `آل عمران ۱۵۶`. جدول **وقف روی / ابتدا از** می‌آید.

توقف سرور: `Ctrl+C`

پورت دیگر:

```bash
python3 -m engine serve --port 9000
```

**فقط باز کردن `web/index.html` با دابل‌کلیک کافی نیست.** مرورگر نمی‌تواند پایتون را صدا بزند. باید همان فرمان `serve` بالا را بزنی تا HTML به موتور وصل شود.

## ۳) خط فرمان، بدون مرورگر

```bash
python3 -m engine "بقره ۹۱"
python3 -m engine "آل عمران 156"
python3 -m engine "همه وقف لازم"
```

## چه چیزی داخل موتور است

- هر ۶۲۳۶ آیه و علائم وقف
- شماره صفحهٔ مصحف ۶۰۴صفحه‌ای
- اصول معنایی محشی
- یادداشت کامل بقره ۹۱ و آل عمران ۱۵۶
- کارت تطبیق: وقف روی … → ابتدا از …
