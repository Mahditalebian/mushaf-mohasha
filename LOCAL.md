# استفادهٔ محلی بعد از کلون

فقط **پایتون ۳.۱۰ یا بالاتر** لازم است. بستهٔ اضافه نمی‌خواهد.
اینترنت برای پرسش لازم نیست. سؤال در ریپو و در گیت‌هاب ذخیره نمی‌شود.

## ۱) کلون

```bash
git clone https://github.com/Mahditalebian/mushaf-mohasha.git
cd mushaf-mohasha
```

اگر از قبل داری:

```bash
cd mushaf-mohasha
git pull
```

## ۲) یک‌بار چک کن که همه چیز آمده

```bash
python3 -m engine check
```

باید ۶۲۳۶ آیه، جدول محشی و صفحهٔ HTML را تأیید کند.

## ۳) صفحهٔ HTML (پیشنهادی)

از **ریشهٔ همین پوشه**:

```bash
python3 -m engine serve
```

یا روی لینوکس / مک:

```bash
chmod +x run.sh
./run.sh
```

روی ویندوز: `run.bat` را دابل‌کلیک کن، یا:

```bat
py -3 -m engine serve --open
```

بعد در مرورگر:

[http://127.0.0.1:8765](http://127.0.0.1:8765)

آیه را بنویس، مثلاً `آل عمران ۱۵۶`. چهار بخش می‌آید:

1. دلیل وقف
2. دلیل وصل
3. دلیل ابتدا
4. حالات اشتباه

در هر وقف ستون **ابتدا از** هم هست.

توقف سرور: `Ctrl+C`

پورت دیگر:

```bash
python3 -m engine serve --port 9000
```

**فقط باز کردن `web/index.html` با دابل‌کلیک کافی نیست.** مرورگر نمی‌تواند پایتون را صدا بزند. باید `serve` را بزنی.

## ۴) خط فرمان، بدون مرورگر

از ریشهٔ ریپو:

```bash
python3 -m engine "بقره ۹۱"
python3 -m engine "آل عمران 156"
python3 -m engine "یونس ۶۵"
python3 -m engine "همه وقف لازم"
```

## چه چیزی داخل موتور است

- هر ۶۲۳۶ آیه و علائم وقف
- شماره صفحهٔ مصحف ۶۰۴صفحه‌ای
- جدول محشی حسینی (وقف روی / ابتدا از)
- اصول معنایی محشی
- استدلال چهاربخشی ساخته‌شده از همین داده‌ها
- یادداشت ثابت فقط برای آیاتی که از قبل در `ayat/` نوشته شده (الان بقره ۹۱ و آل عمران ۱۵۶)

## چه چیزی نوشته نمی‌شود

سؤال و جواب جلسه در ریپو نمی‌رود. ریپو منبع است؛ جلسه مصرف‌کننده است.
