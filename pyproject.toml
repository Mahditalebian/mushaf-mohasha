[project]
name = "mushaf-mohasha"
version = "0.2.0"
description = "موتور محلی وقف و وصل و ابتدا بر پایهٔ مصحف محشی"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools.packages.find]
include = ["engine*"]
